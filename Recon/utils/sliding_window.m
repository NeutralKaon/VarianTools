function out = sliding_window(in, w, d)
% SLIDING_WINDOW
%
%   out = sliding_window(in, w, d)
%
% inputs:
%   in:     array
%   w:      window size (integer)
%   d:      dimension to perform sliding window on
%
% output:
%   out:    cell array (1,N) containing windowed arrays
